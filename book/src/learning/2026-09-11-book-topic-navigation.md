# 2026-09-11：按主题整理学习记录目录

**请求与评审：** 用户再次提示 Claude Code Live 有新 Book 意见。接口仍返回 `Transport closed`，本次从同项目本地会话读取了 2026-09-11 08:25（北京时间）的完整答复。Claude 独立复现了 worktree 附录、RC 三阶段和五个探针，认可路径与历史报告来源问题已处理；剩余意见是学习记录目录持续增长，尚未按主题归并。

**本次修改：** 将此前平铺的 22 条记录归入[工作台与电路实操](workbench-circuits.md)、[语言设计与 RFC](language-rfc.md)、[教材维护与验证](book-maintenance.md)三个主题。每个主题都有导读和记录索引，侧栏使用可折叠子目录；本条记录归入教材维护主题。保留原始文件名、正文与链接，不用主题摘要替代原始证据。

**维护方式：** 新记录继续逐条保存在 `learning/`，同时链接到主题页与 SUMMARY 对应子目录。进度表和记录模板保持直接可见。教材检查、代理实验和学习者理解仍分别记录。

**实际核对：** 修改前后哈希比较确认，22 条历史记录、两份 RFC、三个 RC fixture 源码及十个课程文件共 37 个文件未变。当前 23 条日期记录在 SUMMARY 各出现一次，并分别与三个主题页的 9/11/3 条记录对应。`mdbook build book`、`python3 book/tools/check_book.py`、`git diff --check` 通过；Book 检查覆盖 59 章、62 个 HTML 页面和 3043 个本地链接/资源/锚点。新主题页和旧 R1 记录的本地 HTTP 访问成功。生成目录含三个原生折叠按钮，主题默认不带展开标记。浏览器连接返回 `Transport closed`，本次未实测点击交互；没有将静态核对记作浏览器交互通过。

**范围：** 本次修改目录、索引与维护说明。没有修改中英文 RFC、可执行课程示例或编译器；没有新增学习者操作、复述、硬件测量，也没有把 Proposed M2 标为实现完成。08:25 之后的“看看 RFC 呢”请求尚未读到新的评审答复。
