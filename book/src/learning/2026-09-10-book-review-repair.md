# 2026-09-10：修复 Book 导航并验证 RC 教学课

**问题与决定：** 用户要求按最新 Claude Live 评审及核实后的建议修复。保留 demo→手表路线，将当前语言教学与研究提案分区；更新中文导读，历史译本明确标为过时且默认折叠。英文仍为 Proposed，A/B 未选定。原中文 RFC 源文不作同步翻译。

**教材：** 新增[十路 RC 实操](../course/03-rc-workflow.md)与[诊断指南](../cohdl/diagnostics.md)。第 3–7 课主页面标为课程提纲，RC 实操独立可读。学习记录继续逐条保留；模板区分硬件知识缺口、语言表达困难与有证据的误解修正。

**实际环境：** 从 `/private/tmp/cohdl-main-subdesign-ab44257` 重新执行 cargo build。git HEAD 为 `0e3d7705ab393a5c64c20836540ad1bc2e49898e`、tracked changes 为空，版本 `cohdl 0.7.0 (aarch64-apple-darwin)`，二进制 SHA-256 为 `127ceb68f0c486c711b628f256de1de08e52ffe1c48963c16092012b25b11ff3`。目录名不是源码提交证据；本次结果显式保存提交、版本及文件哈希。源码与二进制的对应来自本次实际构建，验证脚本中的调用者声明本身不证明该对应。

**实际操作与结果：** 在临时项目中运行 `docs/proposals/fixtures/m2-programmability/rc-workflow/verify.py`，使用锁定的 connectors 0.1.6、passive 0.2.2、std 0.3.0。

| 阶段 | 元件 / 无源件 / 网络 | 核对结果 |
| --- | --- | --- |
| 十路 | 30 / 20 / 21 | 每个端点、BOM 与位置符合模型 |
| 第七路电容覆盖 | 30 / 20 / 21 | 仅该电容从 (61,15) 移到 (62,17) mm |
| 十二路保留覆盖 | 36 / 24 / 25 | 原实例位号、位置及限制到原端点的连接保持 |

三个阶段各重复构建两次，产物与锁文件字节一致。五个独立探针：坐标单位错 E1007；电容 B 漏接 E701；删除外部输出 E1302/E701；数组越界 E202；额外短接两路输出 check 通过、无诊断，网络 21→20，精确核对为两个输出分区合并。最初曾预期坐标单位错误为 E110，实际输出纠正为 E1007；没有修改编译器来迎合预期。

**证据：** 仓库 `docs/proposals/fixtures/m2-programmability/rc-workflow/book-results-2026-09-10.json` 保存源码/输入/二进制/验证器哈希、布局、位号和诊断。旧 `results-2026-09-10.json` 保留为上一轮运行记录。验证器每个探针都从原始十路源码重新生成，修改不叠加。

**研发补充：** 本地 `tests/subdesign.rs` 新增数组元素内部放置覆盖回归，比较兄弟器件与网表不变；`cargo test --test subdesign` 25 项通过。英文 RFC 加入接受前对整个 `lib/` 做新旧声明验证差异清单的要求。原型声明验证器尚未实现，这项兼容性审计没有执行。

**未验证与学习状态：** 新增的是教程与自动实验，没有学习者独立操作或复述证据，没有硬件测量、KiCad GUI/物理 DRC、布线或制造结果。M2 候选语法没有运行。下一次带学从一路 RC 的输入、电阻、输出、电容与地开始，先让学习者预测再执行修改。


**评审后的修正：** 三项独立只读审查覆盖事实、技术与结构。将“十个子设计产生三十件”改为“十路设计共三十件”；验证器补上具体制造商/MPN/封装与分组位号的断言、完整诊断集合，以及每次 check/build 后保持原始依赖锁的断言，并重新复验通过。课程增加每阶段快照和布局差异读取，编译器工作树丢失时可从本地仓库检出固定提交；进度页历史更新另设分区。技术与结构复核确认发现已覆盖。


**教材命令复验：** 提取本课五段 shell 命令，在新临时练习目录按顺序执行，退出 0；手动快照比较只发现第七路电容从 (61,15) 到 (62,17)，扩容阶段原器件无移动。另在临时测试目录验证本地 clone 与固定提交 checkout 的恢复流程。Book 构建与本地链接检查通过，Rust 格式检查通过；本地 3001 端口的新实操页面返回 HTTP 200。这里核对的是生成 HTML 与访问，不是 KiCad GUI 或用户学习验收。
