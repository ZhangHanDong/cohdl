# 2026-09-10：核对最新 main，恢复 RFC-032 依据

用户明确要求：拉取最新 main，若其中包含 RFC-032，以 main 的当前内容为准。

已在干净的独立检出 `/private/tmp/cohdl-main-subdesign-ab44257` 执行 `git pull --ff-only origin main`，从 `ab44257` 前进到 `0e3d7705ab393a5c64c20836540ad1bc2e49898e`。工作目录中的文档分支在本次开始时已是 `89e891d`，包含此前的 main 合并；本次没有再次合并或改动其已有未提交内容中的无关部分。

确认 main 的 `docs/design/rfc-032-subdesign.md` 标为 Accepted（2026-09-08），note 10 已收录。该 RFC、note 10、subdesign 检查实现及专项测试相对旧 main 快照未变。此处 RFC-032 与文档分支中的 `docs/proposals/rfc-032-proof-gated-agent-harness.md` 是不同文件、不同主题；后者是用户此前说明 PR 已关闭的历史提案。此前据同一编号排除 subdesign 的判断有误，现撤回。

在最新 main 实际执行 `cargo test --test subdesign --offline --locked`，结果为 **24 通过、0 失败、0 忽略**。覆盖端口合并、义务检查、数组、嵌套、整体布局、位置覆盖、旋转/翻面、稳定输出和静态失败等；本次没有重跑全套测试或硬件实验。

中英文 M2 恢复 RFC-032 的规范依据，原则指南、Book 解读和旧提案编号说明同步修正。历史学习/评审记录保留当时操作和原始实验，顶部注明已撤回的排除判断。M2 实施准备计划标为待组合修订后更新，尚未执行。

专家关于标签、placement 和 fn/subdesign 重叠的问题，现在要放在已接受的组合模型上评审：subdesign 已有端口、具名层级、默认相对布局、整体放置和内部覆盖；循环标签在草案中仅是生成位置的身份。M2 必须补齐与这些既有行为的组合，包括数量参数、作用域、端口义务及包含 subdesign 的预算遍历。将 fn 改成纯计算仍是候选变更，当前规范继续保留其电路构造能力。

下一次完整设计对照采用：定义 RC 通道 → 实例化十路 → 端口接线 → 独立布局 → 覆盖第七路电容 → 扩展到十二路 → 核对原有路径、位号、连接和位置。该 M2 流程尚未实现或运行，不能用本次 24 项既有测试替代。

另保留两个规范/实现差异：RFC-032 的同包 fn 表述与现有跨包 `pub fn` 能力不一致；规范要求的公共层级 IR 不能仅由当前平铺 IR 中的层级路径证明。二者须按治理流程处理，不据此否定 Accepted 状态。

阅读入口：[M2 解读](../cohdl/programming-proposal.md)、[RFC 中文全文](../cohdl/programming-rfc-zh.md)、[subdesign 实验](../cohdl/subdesign.md)。本次是依据校正和现有实现验证，未新增学习者掌握、布局实操或制造测试结果。
