# 2026-09-12：M2 选定 A，归档 B 实施计划

## 实际决定

解释 A/B 差别并建议先做 A 后，用户回复“好，按你的建议继续”。因此本轮把[英文 M2 RFC](../cohdl/programming-rfc-en.md)收敛到 A，并同步[中文全文](../cohdl/programming-rfc-zh.md)及[导读](../cohdl/programming-proposal.md)。这是提案范围的选择；M2 仍为 Proposed，没有正式接受或实现。

A 要让数量、索引与放置坐标可计算，接线、nc、fn 调用及合法布局中的放置可重复。fn 继续可以创建自己的器件。具名的普通器件数组和 subdesign 数组保留现有引用方式；循环标签不成为公开电路或布局分组。

直接在循环体声明 inst/subdesign 的 B 方案延后。英文中的作用域、布局、预算例子和验收表已按 A 调整：这类声明应被拒绝，空循环也不例外；合法 fn 调用中的普通局部声明仍保留。需要顶层调整第七路电容时，采用[RC 实操](../course/03-rc-workflow.md)已有的具名 subdesign 数组和内部位置覆盖。

## 本次沉淀

- 正式的范围记录保存在仓库 `docs/proposals/decisions/2026-09-12-m2-scope-a.md`，包含来源、选择代价与归档文件校验值。
- 选择前的英文、中文和旧实施计划分别保存在 `docs/proposals/archive/2026-09-12-m2-before-a-selection.md`、同名 `.zh-CN.md` 和 `2026-09-12-m2-plan-before-a-selection.md`。
- 旧计划入口已替换为归档说明；新的准备计划是 `docs/superpowers/plans/2026-09-12-m2-a-readiness.md`。它不再安排 B 的循环局部构造，也不再沿用“只加强新循环检查”的旧规则。

下一步需要明确标签/区间拼写，保留词法兼容性取舍，并用原型验证器测量整个 lib/ 的统一声明检查差异，然后走正式接受流程。用户选择 A 没有替代这些决定和实测证据。

## 验证与学习边界

本次仅修订文档。`mdbook build book` 和 `python3 book/tools/check_book.py` 通过：62 章、65 个 HTML 页面、3194 项本地链接/资源/片段；`git diff --check` 通过。中英文均为 654 行、43 个标题、6 个相同代码块，表格结构、行内技术标记、错误码和 URL 对齐。三份归档的 SHA-256 与范围记录一致。

三项只读复审检查了语义、源码事实/准备计划和中英文/Book 一致性，未发现阻塞项；已修正审计章节指向及“当前已有表达式机制”的过强表述。结果保存在仓库 `docs/reviews/2026-09-12-m2-scope-a.md`。可执行 RC 课程源码未改动，没有运行新的 M2 实验或上游 tscircuit 测试。

最终重检曾遇到 HTML/资源短暂缺失；核对发现本仓库的预览服务也写入同一 html 目录，未再改源码的随后检查通过。为排除同时重建的干扰，另向独立临时目录构建，并使用同版检查器及章节清单验证，结果同样通过。预览服务保持运行。

已有 RC 十路→单件覆盖→十二路实验仍是当前语法的证据；A 的循环写法尚不能作为已执行教材。学习者继续处于第 1 课的器件与焊盘理解阶段，没有新增理解验收、板上操作、制造或硬件测量结果。当前状态见[进度](progress.md)。
