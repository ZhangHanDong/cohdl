# 2026-09-07：课程起点

状态：路线与教材准备；Konnect 实时操作、PCB 修改、制造、实板测试均未执行。

## 已确认的目标

用户希望通过本地 Konnect，从 KiCad demo 学习 PCB，再设计手表主板；同时将实际设计需求用于 CoHDL 的组合、编程、参数化与契约里程碑。用户选择了推荐范围：屏幕、蓝牙、运动传感、电池充电，先做可调试原型。

用户进一步要求用 mdBook 组织课程，并根据学习过程逐步完成 CoHDL Book。因此建立了当前课程、知识和学习记录三个相互链接的区域。

项目名称统一为 CoHDL。用户已将本地学习资料的父目录改名为 `/Users/zhangalex/Work/CoHDL/`；本次核对该目录及其中的 `Konnect`、`kicad-demos` 均存在，课程路径已同步更新。`watch-lab` 仍待第 0 课创建。

## 已完成的资料工作

- 阅读本地 Konnect 的 README、工具目录和相关实现；模型由客户端选择，Konnect 提供 MCP 到 KiCad 的能力。
- 上次环境检查得到 KiCad CLI 10.0.6；没有建立 Konnect MCP/IPC 连接。开始第 0 课时重新核对当前状态。
- 使用 KiCad CLI 导出并读取 sonde 与 LM317 的 XML 网表。sonde 原理图中的 R1 pin 1 与 D1 pin 2 同网，R1 pin 2 与 J1 pin 15 同网；这不代替 PCB 铜连接核对。
- 核对 SF32 样例：已有显示与 IMU 电路，但缺充电与蓝牙天线实现，仍需布局布线；具体限制见 [第 5 课](../course/05-watch-prototype.md)。

网表可在第 0 课建立副本后重新导出；例如 sonde 使用以下命令，输出放到练习目录，覆盖前先保留需要的旧结果：

```sh
"/Applications/KiCad/KiCad.app/Contents/MacOS/kicad-cli" sch export netlist \
  --format kicadxml \
  --output "$LAB_ROOT/lessons/01-sonde/sonde.xml" \
  "$LAB_ROOT/lessons/01-sonde/sonde xilinx.kicad_sch"
```

变量由 [第 0 课](../course/00-workbench.md) 定义。历史临时导出文件位于本机 `/tmp`，不作为读者必须拥有的课程输入。

## 教材例子的实际检查

基线 CoHDL 0.6.0 / `9ac0e3c`，本次从源码离线构建编译器。以下命令均在仓库根目录执行并得到退出码 0：

```sh
target/debug/cohdl check book/examples/connections.cohdl --no-std
target/debug/cohdl fmt book/examples/connections.cohdl --check
```

还在临时副本删除 RETURN 网，实际观察到 E701 分别指出 `Connections::input.B` 和 `Connections::output.B`，退出码为 1；临时副本随检查结束自动清理，原例子保持不变。

这是 [连接模型](../cohdl/connections.md) 中教学源码的检查，不涉及选料、PCB 生成或真实器件性能。

教材使用 mdBook 0.5.3 构建成功；本地链接、资源、锚点和目录覆盖检查通过。本地预览服务的首页、认图课、连接模型和进度页均得到 HTTP 200。Browser 报告没有可用浏览器，因此真实点击、搜索和视觉阅读检查尚未执行。三项独立内容审阅已完成，并修正了前置路径、检查成功含义和排除项核对方式。

## 当前仍待完成

Konnect 安装/连接核对、练习副本、GUI 认图、移动与修复、用户复述均未执行。最终 MCU、屏幕、电池、板框、固件与制造平台尚未选定。

下一项是 [第 0 课](../course/00-workbench.md)。完成后新增一条实操记录，并更新 [进度表](progress.md)。
