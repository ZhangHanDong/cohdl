# 2026-09-08：起草 M2 编译期可编程能力 RFC

用户确认 subdesign 对应此前第一优先级，要求先为第二优先级的 for/数组接线能力起草 RFC，并强调 cohdl.dev 的整体一致性原则。

本次继续以 main `ab44257` 为事实基线，远端 main hash 已重新核对。阅读 RFC 流程、Constitution、原则约束、设计回归，以及数组、泛型、fn、subdesign、单位、布局和诊断实现；没有修改 Accepted 规范或编译器。

起草 `docs/proposals/rfc-draft-bounded-compile-time-programming.md`，状态 Draft / Proposed，正式编号未分配。推荐有限循环加共用整数/Length 表达式；包括明确的 Accepted RFC 修订点、两阶段检查、循环作用域、身份、精确计算、展开上限、诊断、工具影响及未来验收表。用户尚未接受其中的具体语法、限制或实施方案。

实际写出当前语法的十颗 LED 对照源：人工定义的简化接口，无采购 part，仅运行 main 的 `check --no-std --json`，pass、无诊断。未来表达另存 `.cohdl.txt`；临时复制成 .cohdl 后运行同一 check，按预期失败。原始命令、结果、源码及二进制哈希位于 `docs/proposals/fixtures/m2-programmability/baseline-results.json`。另用 main 检查后的 IR 核对完整连接分区、NC 和放置，结果存 `reference-topology.json`。这是建立对照目标，不是 M2 已实现；未来锁和构建验证还需有真实 part 的样例。

三路只读复核发现并修订了几个重要问题：序号式循环身份可能静默转移位号，改为提议显式循环名；预算必须计入 helper 内普通实例及字面量数组；新静态检查限定到新能力涉及的区域，不能未经说明改变所有旧 unused 定义的 verdict；参考与未来源统一设计名，区分 IR 对照与构建产物验收。所有改动仍为 Proposed，未获语言设计接受。

配套 [M2 草案解读](../cohdl/programming-proposal.md)已区分当前基础、提议语法和未来验收。没有新增电气测量、制造结果或学习者独立掌握证据。
