# 2026-09-10：根据 Live 评审修订英文 RFC

用户要求先以英文版 RFC 为准开始修改。已通过 mempal 读取 Claude Code 本项目 Live 会话的评审，并对照 main `0e3d770` 核对；评审来源为本机 `.claude/projects/-Users-zhangalex-Work-Projects-cohdl/d353a61e-4076-4f7e-a2e8-dc70afcb5b67.jsonl`，评审时间为 2026-09-10 04:15 UTC。

英文正文已撤回循环内直接 inst 的推荐，明确 A（既有对象/数组/subdesign 上的操作循环，保留产生电路的 fn）与 B（加直接局部构造）尚未选定。数量参数、常量、索引和布局所属关系同时覆盖 fn/subdesign；预算发现遍历完整的调用与包含图，计量也包含逻辑节点、端口和连接块，消除了原来只遍历 fn 的规则。

统一声明检查作为明确的提议取代长期保留旧 helper 检查缺口的方案；兼容性章节说明此前未调用、但存在静态错误的定义可能新被拒绝。fn 纯计算化仍未被接受，标签与半开 fan-out 的范围选择也未定案。区间端点差异、取余运算的空格边界及 E201 注册表描述分别明示。

三个场景现在包含完整 RC 流程：定义通道 → 实例化十路 → 端口接线 → 独立布局 → 改第七路电容 → 扩到十二路。为验证既有能力，新增 `docs/proposals/fixtures/m2-programmability/rc-workflow/`，使用真实库物料和当前字面量语法。十路/十二路分别得到 30/36 个元件、21/25 条网络；十路中的 20 个无源元件扩展为 24 个，其余是连接器。每阶段构建和重复字节检查通过，位号和原有端点之间的连接关系保留；覆盖编辑只改变第七路电容位置，扩容没有移动原有元件。这些结果不等于拟议循环执行成功，也不构成制造检查。

三路独立英文审查未发现阻塞性矛盾；已修正场景编号 B 与候选 B 重名的读者歧义。中文版源文件未修改，Book 给旧译本及旧解读增加版本提示，并提供独立[英文入口](../cohdl/programming-rfc-en.md)。这是 Proposed 文档修订与现有编译器基线实验，没有新增语言实现、学习者掌握或硬件实测。
