# 2026-09-10：根据新评审简化课程环境准备

**触发：** 用户提示 Claude Code Live 有新的 Book 评审。本次读取北京时间 15:06 的答复：它确认中英文同步通过，进一步指出 RC 教程不应保留代理机器上的临时工作树路径，并建议使用独立 worktree。

**核实与决定：** 当前课程确有该路径。本地能找到已验证的完整提交 `0e3d7705ab393a5c64c20836540ad1bc2e49898e`，但没有 `v0.7.0` 标签。因此使用固定提交创建独立工作树，路径由读者仓库位置推导。将构建与故障处理移入[课程编译器准备](../appendix/course-compiler.md)，[RC 正文](../course/03-rc-workflow.md)从复制电路开始；补明 Python 3.11 要求和缺包处理。

**实际执行：** 在新的测试仓库中创建课程工作树，从中构建 CoHDL 0.7.0。提取附录和课程的全部 7 个 shell 代码块，以 `zsh -e` 在同一终端环境依次执行，退出 0。原工作区未切换分支。Cargo 依赖及 CoHDL 包在本机已有缓存；本次没有验证首次联网下载或缺包安装流程。

| 阶段 | 真实元件 / 无源件 / 网络分区 | 核对 |
| --- | --- | --- |
| 十路 | 30 / 20 / 21 | 端点、物料、位置及重复构建一致 |
| 十路，单件覆盖 | 30 / 20 / 21 | 仅第七路电容从 (61, 15) 移到 (62, 17) |
| 十二路，保留覆盖 | 36 / 24 / 25 | 原位号及受限端点连接保持，无旧元件移动 |

五个探针分别得到 E1007、E701、E1302/E701、E202，以及输出短接时 check 通过但网络 21→20。结构检查通过仍不等于通道独立性正确。新运行报告独立保存到仓库 `docs/reviews/evidence/2026-09-10-book-portable-setup.json`，保留本次源码提交和二进制哈希。

**其他评审意见：** 早期 `results-2026-09-10.json` 不补写事后来源信息，两版 RFC 已明确以带完整来源的 `book-results-2026-09-10.json` 作为后续证据。学习记录目录的主题化整理仍待后续，本次保留所有实际记录与入口。没有修改诊断分类或 M2 的 Proposed 状态。

**学习与硬件边界：** 这是代理复验课程准备流程，没有新增学习者独立操作、复述或硬件测量。学习进度仍以原有证据为准。
