# M2 RFC 中英文全文同步复核 — 2026-09-10

## 范围与权威

用户要求中文 RFC 与当前英文全文对齐。英文是工作权威；本次没有选择 A/B、接受 M2 或实施语言功能。英文只更新译本状态和现有 RC 实验的证据入口，规范正文的设计选择保持不变。

| 文件 | SHA-256 |
| --- | --- |
| `docs/proposals/rfc-draft-bounded-compile-time-programming.md` | `8f6646073234d43f52be73d59d9cb5f1bdd73f065cb60fa7fcdbe550ba508a8e` |
| `docs/proposals/rfc-draft-bounded-compile-time-programming.zh-CN.md` | `70e7b96ca7a7303069090f455fb260b812b1f137bd2e5b6cd270ffbd4b1bc1f4` |
| `docs/proposals/archive/2026-09-10-m2-before-live-review.zh-CN.md` | `ecd0b4eed3cd4ffdb71c9db94981c99d734113f57f8811c8aba1425d50c916d7` |

旧中文全文完整归档；学习记录保留当时的状态。Book 的当前中文全文直接包含同步后的文件。

## 机械核对

修正后重新比较两份全文：

- 均为 643 行、43 个标题，标题层级逐项相同。
- 14 张表的行数逐项相同：`9, 8, 7, 9, 8, 9, 6, 6, 10, 9, 7, 12, 3, 38`。
- 6 个 fenced code block 字节一致，代码及其中英文注释保持原样。
- 行内代码出现 157 次、错误码出现 45 次、来源 URL 出现 4 次；分别比较内容及每项出现次数，全部一致。
- 当前全文、导读、首页、目录和维护说明已清除“尚未同步/过时译本”标签；历史记录明确限定为当时的状态。

这些核对只证明结构与保留字面量一致，不能代替语义复核。

## 三轮独立语义复核及修正

三个只读评审分别检查规范语义、事实/数值和中文编辑/Book 导航。均已完成全文对照，未发现遗漏章节、A/B 反转或将 Proposed 误写成 Accepted/已实现。以下反馈经对照英文后修正：

| 位置 | 问题 | 处理 |
| --- | --- | --- |
| 约束性基线 | `Placement alone` 译为“只有布局”，放大了访问权限 | 改为“只有放置操作可以深入引用内部实例” |
| 目标、退出条件标题 | `gradeability` 译为“可判定性”，易与可判定问题混淆 | 分别改为“机器可检验性”“可检验性” |
| 分阶段检查说明 | `invariant errors` 译为“不依赖具体值的错误”，过窄 | 改为“在信息足够时提前拒绝已知必然发生的错误”；实际绑定 N 后发现空循环中的 `1/N` 仍在规则范围内 |
| 现有基线证据 | 中文将“保持不变”同时附着到 BOM 字段和依赖锁 | 明确分别核验 BOM 字段、确认依赖锁未变；不暗示扩容前后 BOM 相同 |
| 预算验收行 | `Exactly/one over` 的“超过一次”没有表达上限加一 | 改为“分别恰好达到上限及比上限多 1” |

其余重点已对齐：fn 保留构造效果；循环不改变布局所有权；完整引用图发现与实际展开计量分开；统一声明验证独立生效；空循环的静态/绑定/迭代检查分层；类型正确不保证意图拓扑正确；诊断 schema v1 与 API docs v2 分开；接受前须做整个 `lib/` 的兼容性差异审计，当前没有声称该审计已运行。

## Claude Code Live 新 Book 评审的处理

读取来源为本地 Claude 会话 `d353a61e-4076-4f7e-a2e8-dc70afcb5b67.jsonl` 中 `2026-09-10T06:41:17.115Z` 的答复（北京时间 14:41）。Mempal 连接返回 `Transport closed`，因此使用只读本地会话回退；未向外部会话发送消息。

| 新意见 | 核实与处理 |
| --- | --- |
| 不能从临时工作树目录名判断编译器提交 | 接受；现有新报告记录完整源码提交、版本和可执行文件哈希。Claude 也复现了 25 项 subdesign 测试及 RC 三阶段/五探针 |
| RFC 应指向有完整编译器来源的新证据 | 已在两版 RFC 中区分早期 `results-2026-09-10.json` 与后续 `book-results-2026-09-10.json`；不事后改写旧运行报告 |
| 库兼容性变更需要单列成本 | 当前英文设计 §8 已有整个 `lib/` 旧/新验证器差异审计要求，本次完整译入中文；原型及审计未运行 |
| 环境准备命令过重 | 记录后续将准备流程移到独立附录的建议；直接在当前仓库 `git checkout v0.7.0` 不适合存在未提交改动的工作区 |
| 学习记录的目录条目持续增长 | 记录后续按主题组织入口的建议；仍须保留实际记录及 SUMMARY 链接，不删除证据或虚构学习完成 |
| 考虑统一 E1007/E110 等诊断分类 | 属于独立语言设计议题；翻译同步保留当前英文规则和实际观测码，不擅自修改 Accepted 契约 |

本次未重新运行编译器或硬件实验；引用的是已保存的实际报告。新增工作为翻译、版本提示、证据索引和 Book 文档验证。

## 文档验证结果

修正后 `mdbook build book`、`python3 book/tools/check_book.py` 和 `git diff --check` 均通过。Book 检查覆盖 53 章、56 个 HTML 页面和 2757 个本地链接/资源/锚点。当前入口的过时标签检索无匹配。构建中的一次 HTML 文件读取竞争发生于本地预览同时重建，输出稳定后复查通过；没有将该瞬时失败记作章节缺失。
